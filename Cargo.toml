[package]
name = "tauri-axum"
version = "0.1.1"
edition = "2021"
authors = ["Logan Keenan"]
description = "Run Axum web applications within Tauri, enabling server-side rendering patterns by running the Axum app in the Tauri backend."
license = "MIT"
repository = "https://github.com/sternelee/tauri-axum"
include = [
    "src/**/*",
    "readme.md",
    "LICENSE.md"
]

[lib]
path = "src/lib.rs"

[dependencies]
axum = "0.8.4"
serde = { version = "1.0.219", features = ["derive"] }
thiserror = "2.0.16"
tower-service = "0.3.3"
futures = "0.3"
tokio = { version = "1.47", features = ["sync", "rt", "macros"] }
tokio-stream = "0.1"
tauri = { version = "2", optional = true }

[features]
default = []
streaming = ["tauri"]

[dev-dependencies]
tokio = { version = "1.47", features = ["full"] }
serde_json = "1.0"
axum = "0.8.4"
serde = { version = "1.0.219", features = ["derive"] }
tauri = { version = "2", features = [] }
tower-service = "0.3.3"

